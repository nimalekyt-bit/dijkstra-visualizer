import sys
from pathlib import Path
from build_coursework_note import (
    Document, configure_document, add_title_page, add_contents_page,
    add_heading, add_paragraph, add_table, add_numbered, add_bullets, add_code_block,
    DOCX_PATH, _TABLE_COUNTER, COURSEWORK_TITLE, PROJECT_TITLE
)

def build_docx_massive():
    doc = Document()
    _TABLE_COUNTER["n"] = 0
    configure_document(doc)
    add_title_page(doc)
    add_contents_page(doc)

    # ===================================================================
    # 2. ВВЕДЕНИЕ
    # ===================================================================
    add_heading(doc, "2. Введение", 1)
    add_paragraph(
        doc,
        "В современном информационном обществе и эпоху глобальной цифровизации задачи оптимизации "
        "и эффективного поиска маршрутов играют фундаментальную роль во множестве научно-технических "
        "и прикладных дисциплин. Классическая задача поиска кратчайшего пути во взвешенном графе — "
        "это нахождение маршрута наименьшей суммарной стоимости между двумя или более заданными "
        "вершинами, при условии, что веса рёбер отражают определенную метрику (расстояние, время "
        "выполнения, финансовые затраты или уровень потерь). Эта математическая модель "
        "повсеместно применяется при маршрутизации пакетов данных в компьютерных сетях (в частности, "
        "в протоколах OSPF и IS-IS), в спутниковой навигации (GPS/ГЛОНАСС), в транспортной логистике, "
        "а также в системах искусственного интеллекта и планирования действий."
    )
    add_paragraph(
        doc,
        "Известными и наиболее распространенными методами решения данной проблемы являются: поиск в "
        "ширину (эффективен исключительно для невзвешенных графов), алгоритм Дейкстры (для графов "
        "строго с неотрицательными весами рёбер), алгоритм Беллмана-Форда (допускающий отрицательные "
        "веса и способный выявлять отрицательные циклы), алгоритм Флойда-Уоршелла (для поиска путей "
        "сразу между всеми парами вершин) и алгоритмы эвристического поиска, такие как A*, "
        "применяемые в основном в игровых движках и геоинформационных системах. По заданию на данную "
        "курсовую работу основным и базовым алгоритмом выбран метод Дейкстры."
    )
    add_paragraph(
        doc,
        "Выбор алгоритма Дейкстры обусловлен его доказанной математической строгостью, оптимальной "
        "полиномиальной временной сложностью (особенно при использовании очереди с приоритетами) и "
        "высокой наглядностью процесса релаксации рёбер, что делает его превосходным объектом для "
        "визуального изучения. В случае применения этого алгоритма гарантируется нахождение глобально "
        "оптимального маршрута в графах с неотрицательными стоимостями переходов."
    )
    add_paragraph(
        doc,
        "Целью настоящей курсовой работы является самостоятельная программная реализация "
        "алгоритма Дейкстры, а также проведение его сравнительного анализа с алгоритмами Беллмана-Форда "
        "и Флойда-Уоршелла. Алгоритм Дейкстры и базовая для него структура данных «очередь с "
        "приоритетами» реализованы вручную, без применения готовых библиотечных абстракций, что "
        "гарантирует глубокое понимание низкоуровневых механизмов работы с памятью и структурами данных. "
        "Для обеспечения наглядности результатов работы создано кроссплатформенное интерактивное "
        "веб-приложение с использованием HTML5, CSS3 и JavaScript. Вспомогательные библиотеки "
        "(Cytoscape.js, Chart.js и jsPDF) применяются исключительно для отрисовки графических "
        "компонентов, графиков производительности и формирования итоговой отчетности, не затрагивая "
        "ядро вычислительной логики."
    )

    # ===================================================================
    # 3. КРАТКОЕ ОПИСАНИЕ МЕТОДОВ И АЛГОРИТМОВ
    # ===================================================================
    add_heading(doc, "3. Краткое описание используемых методов и алгоритмов", 1)
    
    add_heading(doc, "3.1. Основные понятия теории графов", 2)
    add_paragraph(
        doc,
        "Граф представляет собой нелинейную структуру данных, задаваемую как пара множеств G = (V, E), "
        "где V (vertices) — непустое конечное множество вершин, а E (edges) — множество рёбер, "
        "соединяющих пары вершин из множества V. В контексте решаемой задачи вершины ассоциируются с "
        "физическими объектами или состояниями (города, маршрутизаторы, перекрестки), а рёбра — с "
        "существующими между ними связями. Если связи являются двунаправленными, граф называется "
        "неориентированным. Если направление связи имеет значение (например, дороги с односторонним "
        "движением), граф называется ориентированным или орграфом."
    )
    add_paragraph(
        doc,
        "Для моделирования реальных затрат на перемещение между объектами вводится понятие веса ребра — "
        "числовой характеристики, сопоставляемой каждому ребру множества E. Граф, рёбрам которого "
        "присвоены веса, называется взвешенным. Стоимость любого пути в таком графе определяется как "
        "сумма весов всех рёбер, входящих в данный путь. Задача поиска кратчайшего пути формулируется "
        "как поиск пути между начальной вершиной s и конечной вершиной t, сумма весов которого "
        "минимальна среди всех возможных путей между этими вершинами."
    )
    add_paragraph(
        doc,
        "Существует два основных способа представления графов в памяти вычислительных систем: матрица "
        "смежности и списки смежности. Матрица смежности представляет собой двумерный массив размером "
        "|V|×|V|, где элемент A[i][j] содержит вес ребра от i к j. Данный подход требует O(V²) памяти, "
        "что делает его неэффективным для разреженных графов. В разрабатываемом программном обеспечении "
        "используется представление в виде списков смежности, при котором для каждой вершины хранится "
        "список только смежных с ней вершин. Это сокращает объем требуемой памяти до O(V + E) и "
        "позволяет быстро перебирать соседей текущей вершины, что критически важно для эффективной "
        "работы алгоритмов поиска на графах."
    )

    add_heading(doc, "3.2. Алгоритм Дейкстры", 2)
    add_paragraph(
        doc,
        "Алгоритм Дейкстры, предложенный нидерландским ученым Эдсгером Вибе Дейкстрой в 1959 году, "
        "является жадным алгоритмом, находящим кратчайшие пути от одной выделенной вершины графа "
        "до всех остальных. Основным ограничением алгоритма является требование неотрицательности "
        "весов всех рёбер в графе (w(u, v) ≥ 0). Это связано с тем, что алгоритм помечает вершины "
        "как «окончательно обработанные» в порядке возрастания их расстояния от начальной вершины, "
        "и наличие отрицательных рёбер может нарушить справедливость сделанного выбора."
    )
    add_paragraph(
        doc,
        "В основе алгоритма лежит принцип динамического программирования и процесс, называемый "
        "релаксацией. Релаксация ребра (u, v) состоит в проверке возможности улучшить известный "
        "на данный момент кратчайший путь к вершине v, проходя через вершину u. Изначально расстояние "
        "до начальной вершины s принимается равным нулю (dist[s] = 0), а до всех остальных вершин — "
        "бесконечности (dist[v] = ∞). Множество вершин делится на два непересекающихся подмножества: "
        "S (множество вершин, для которых кратчайшее расстояние уже окончательно найдено) и Q (очередь "
        "необработанных вершин)."
    )
    add_numbered(doc, [
        "Инициализация: для всех v ∈ V устанавливается dist[v] = ∞, prev[v] = null. Для стартовой вершины s: dist[s] = 0. Формируется очередь Q, содержащая все вершины графа.",
        "Пока очередь Q не пуста, из нее извлекается вершина u с минимальным значением dist[u].",
        "Извлеченная вершина u добавляется в множество обработанных вершин S.",
        "Для каждого смежного с u ребра (u, v) выполняется попытка релаксации: если dist[u] + w(u, v) < dist[v], то значение dist[v] обновляется (dist[v] = dist[u] + w(u, v)), а вершина u запоминается как предшественник v (prev[v] = u).",
        "При обновлении расстояния приоритет вершины v в очереди Q пересчитывается (уменьшается).",
        "После исчерпания всех вершин в Q работа алгоритма завершается, и кратчайший путь до любой достижимой вершины может быть восстановлен обратным проходом по массиву предшественников prev."
    ])
    add_paragraph(
        doc,
        "Для эффективного выбора вершины с минимальным расстоянием применяется очередь с приоритетами. "
        "При использовании бинарной кучи время извлечения минимума и изменения приоритета составляет "
        "O(log V). Поскольку каждая вершина извлекается один раз, а каждое ребро релаксируется один раз, "
        "итоговая временная сложность составляет O((V + E) log V). Пространственная сложность алгоритма "
        "оценивается как O(V + E), что связано с необходимостью хранения графа, массивов расстояний и "
        "очереди."
    )

    add_heading(doc, "3.3. Альтернативные методы", 2)
    add_paragraph(
        doc,
        "Алгоритм Беллмана-Форда решает аналогичную задачу поиска пути от одной вершины, однако он "
        "лишен ограничения на неотрицательность весов. Метод основан на многократной релаксации всех "
        "рёбер графа. Выполняется |V| - 1 итераций, на каждой из которых релаксируются абсолютно все "
        "рёбра множества E. Дополнительная |V|-я итерация позволяет выявить наличие циклов с отрицательным "
        "суммарным весом. Данный метод более универсален, но его асимптотическая временная сложность O(V·E) "
        "существенно уступает алгоритму Дейкстры, что делает его непригодным для больших плотных графов."
    )
    add_paragraph(
        doc,
        "Алгоритм Флойда-Уоршелла решает задачу поиска кратчайших путей между всеми парами вершин графа "
        "одновременно. Алгоритм использует подход динамического программирования с построением матрицы "
        "кратчайших расстояний. На каждом из |V| шагов алгоритм проверяет, не является ли путь через "
        "промежуточную вершину k более коротким, чем прямой путь между i и j. Сложность метода "
        "составляет O(V³), что ограничивает его применение графами с числом вершин до нескольких тысяч."
    )

    # ===================================================================
    # 4. ОСОБЕННОСТИ ПРОГРАММНОЙ РЕАЛИЗАЦИИ
    # ===================================================================
    add_heading(doc, "4. Особенности программной реализации алгоритмов", 1)
    
    add_heading(doc, "4.1. Общая структура проекта", 2)
    add_paragraph(
        doc,
        "Программная реализация выполнена в парадигме объектно-ориентированного и событийно-ориентированного "
        "программирования. Приложение представляет собой модульную систему, работающую непосредственно "
        "в браузере пользователя, что обеспечивает высокую кроссплатформенность (Windows, macOS, Linux, "
        "мобильные ОС) и отсутствие необходимости в установке дополнительного серверного окружения. "
        "Архитектура приложения строго разделяет логику обработки данных, математические алгоритмы и "
        "слой представления (пользовательский интерфейс)."
    )
    add_table(
        doc,
        "Назначение основных модулей проекта",
        ["Файл / Модуль", "Описание и назначение"],
        [
            ["index.html", "Корневой файл разметки. Содержит структуру DOM-дерева, модальные окна и подключение скриптов."],
            ["css/style.css", "Определяет визуальное оформление, адаптивность под мобильные устройства и поддержку темной темы."],
            ["js/graph.js", "Модуль управления структурой графа. Реализует паттерн инкапсуляции графовых данных."],
            ["js/dijkstra.js", "Ядро вычислений. Содержит реализацию алгоритма Дейкстры и бинарной мин-кучи."],
            ["js/visualizer.js", "Движок визуализации, управляющий генерацией и воспроизведением снимков состояния (шагов)."],
            ["js/ui.js", "Глобальный контроллер, связывающий события DOM с логикой ядра приложения."],
            ["js/storage.js", "Модуль сериализации и персистентного хранения. Работа с LocalStorage и экспорт данных в файлы."],
            ["tests/unit-tests.js", "Комплекс автоматизированных модульных тестов, проверяющих инварианты работы алгоритмов."]
        ],
        [4.2, 10.3],
    )
    add_paragraph(
        doc,
        "Общая логика программной реализации алгоритма Дейкстры строго соответствует классической схеме "
        "(см. блок-схему алгоритма). На схеме отражены основные этапы: первоначальная инициализация структур "
        "данных, основной цикл извлечения вершины с минимальным приоритетом из очереди, условная проверка "
        "и релаксация смежных рёбер, а также процесс восстановления итогового кратчайшего пути по массиву "
        "предшественников."
    )

    add_heading(doc, "4.2. Используемые типы данных", 2)
    add_paragraph(
        doc,
        "В процессе работы программы используются современные встроенные типы данных JavaScript (ES6+), "
        "обеспечивающие высокую производительность. Для хранения списков вершин и рёбер применяется "
        "объектно-ориентированный подход: созданы классы GraphManager и DijkstraAlgorithm. Внутри "
        "классов активно используются коллекции типа Map и Set."
    )
    add_paragraph(
        doc,
        "Таблица ассоциативных массивов Map используется для хранения дистанций (ключ — строковый ID "
        "вершины, значение — число с плавающей точкой) и массива предшественников (ключ и значение — "
        "строковые идентификаторы). Выбор структуры Map вместо обычных объектов обусловлен лучшей "
        "асимптотикой при частом добавлении и удалении ключей, а также строгим сохранением типов ключей. "
        "Коллекция Set применяется для поддержания списка посещенных вершин (visited), что гарантирует "
        "время проверки принадлежности к множеству за O(1). Собственная реализация класса PriorityQueue "
        "основана на массиве (Array), представляющем бинарное дерево."
    )

    add_heading(doc, "4.3. Организация файлового ввода-вывода", 2)
    add_paragraph(
        doc,
        "В соответствии с обязательными требованиями к курсовой работе, комплексный обмен данными с внешней "
        "средой реализован посредством файлового ввода-вывода. Импорт графов осуществляется через чтение "
        "файлов формата JSON. Входной файл должен содержать флаг ориентированности (directed), массив "
        "вершин (каждая с полями id, label, x, y) и массив рёбер (с полями id, source, target, weight). "
        "Чтение файла реализуется средствами браузерного API FileReader, что позволяет читать локальные файлы "
        "в асинхронном режиме без отправки их на сервер."
    )
    add_paragraph(
        doc,
        "Экспорт результатов алгоритмической обработки производится в двух форматах: JSON (для "
        "машинного анализа) и текстовый TXT или полноценный PDF (для предоставления отчета пользователю). "
        "Выходной файл содержит: итоговый восстановленный маршрут (массив идентификаторов или имен вершин), "
        "общую стоимость маршрута (сумму весов), детальную таблицу кратчайших расстояний от стартовой "
        "вершины ко всем остальным, а также статистику производительности (время расчета в миллисекундах и "
        "количество элементарных операций). Процедура сохранения реализована через генерацию Blob-объектов "
        "с последующим автоматическим инициированием скачивания (паттерн виртуальной ссылки)."
    )

    add_heading(doc, "4.4. Валидация входных данных и тестирование", 2)
    add_paragraph(
        doc,
        "Ключевым аспектом надежности программы является строгая валидация внешних данных. При попытке "
        "загрузки файла метод validateGraphData проводит многоступенчатую проверку: контролируется "
        "целостность JSON-структуры, уникальность всех идентификаторов, отсутствие «висячих» рёбер "
        "(ссылающихся на несуществующие вершины) и корректность весовых коэффициентов (они должны быть "
        "конечными числами, для Дейкстры — строго неотрицательными). Любое нарушение приводит к выбросу "
        "исключения и прерыванию импорта, предотвращая непредсказуемое поведение ядра."
    )

    # ===================================================================
    # 5. СРАВНИТЕЛЬНЫЙ АНАЛИЗ
    # ===================================================================
    add_heading(doc, "5. Сравнительный анализ используемых алгоритмов или методов", 1)
    add_paragraph(
        doc,
        "В рамках курсовой работы был проведен не только теоретический, но и практический сравнительный "
        "анализ различных подходов к поиску кратчайших путей на графах. В приложении реализован модуль "
        "«Производительность», который позволяет запускать алгоритмы Дейкстры, Беллмана-Форда и "
        "Флойда-Уоршелла на случайно сгенерированных графах различных размерностей (от 10 до 1000 вершин) "
        "и фиксировать реальное время их выполнения."
    )
    add_table(
        doc,
        "Сравнительные оценки сложности и ограничений алгоритмов",
        ["Метод", "Ограничения", "Теоретическое время", "Память"],
        [
            ["Поиск в ширину (BFS)", "Только невзвешенные графы", "O(V + E)", "O(V)"],
            ["Дейкстра (бинарная куча)", "Веса рёбер ≥ 0", "O((V + E) log V)", "O(V + E)"],
            ["Дейкстра (массив)", "Веса рёбер ≥ 0", "O(V²)", "O(V)"],
            ["Беллман-Форд", "Допускает веса < 0", "O(V·E)", "O(V)"],
            ["Флойд-Уоршелл", "Все пары вершин", "O(V³)", "O(V²)"],
        ],
        [4.0, 4.0, 3.6, 2.9],
    )
    add_paragraph(
        doc,
        "Практические замеры, проведенные с помощью функции performance.now() стандарта High Resolution Time, "
        "подтверждают теоретические выкладки. На разреженных графах алгоритм Дейкстры с использованием "
        "бинарной кучи демонстрирует подавляющее преимущество, выполняясь за доли миллисекунд даже при "
        "сотнях вершин. В то же время алгоритм Беллмана-Форда, вынужденный производить полный перебор всех "
        "рёбер V-1 раз, показывает заметную деградацию производительности (рост времени выполнения носит "
        "квадратичный характер относительно плотности графа)."
    )
    add_paragraph(
        doc,
        "Алгоритм Флойда-Уоршелла, несмотря на самую высокую асимптотику O(V³), показывает высокую "
        "стабильность за счет простоты внутреннего цикла (отсутствие ветвлений и сложных структур данных). "
        "Однако его пространственная сложность O(V²) делает его неприменимым для графов, содержащих более "
        "десятка тысяч вершин, так как матрица расстояний быстро исчерпывает доступную оперативную память. "
        "Следовательно, для поставленной задачи (поиск пути от одной вершины при неотрицательных весах) "
        "алгоритм Дейкстры является безальтернативно лучшим решением, обеспечивающим идеальный баланс "
        "между скоростью и расходом памяти."
    )

    # ===================================================================
    # 6. ЛИСТИНГИ
    # ===================================================================
    add_heading(doc, "6. Листинги программ с обязательными исчерпывающими комментариями", 1)
    add_paragraph(
        doc,
        "В данном разделе приводятся наиболее значимые фрагменты исходного программного кода, "
        "демонстрирующие реализацию ключевых структур данных и алгоритмов. Полные исходные тексты "
        "находятся в директории 'js' программного комплекса."
    )
    add_heading(doc, "6.1. Класс очереди с приоритетами", 2)
    add_code_block(doc, """
class PriorityQueue {
    constructor() {
        // Хранилище элементов кучи. Каждый элемент представляет собой объект 
        // с полями element (идентификатор вершины) и priority (текущее расстояние)
        this.heap = [];
    }

    // Метод добавления нового элемента в кучу
    enqueue(element, priority) {
        this.heap.push({ element, priority });
        this._bubbleUp(this.heap.length - 1); // Восстановление свойства кучи
    }

    // Метод извлечения элемента с наивысшим приоритетом (наименьшим расстоянием)
    dequeue() {
        if (this.isEmpty()) return null;
        const min = this.heap[0]; // Корень кучи всегда содержит минимум
        const last = this.heap.pop();
        if (this.heap.length > 0) {
            this.heap[0] = last;   // Помещаем последний элемент в корень
            this._sinkDown(0);     // Опускаем его на правильную позицию
        }
        return min;
    }

    isEmpty() { return this.heap.length === 0; }

    // Процедура просеивания вверх (всплытие)
    _bubbleUp(i) {
        while (i > 0) {
            const parent = Math.floor((i - 1) / 2);
            // Если приоритет текущего узла больше или равен родительскому — куча корректна
            if (this.heap[i].priority >= this.heap[parent].priority) break;
            // Иначе меняем узлы местами
            [this.heap[i], this.heap[parent]] = [this.heap[parent], this.heap[i]];
            i = parent;
        }
    }

    // Процедура просеивания вниз (погружение)
    _sinkDown(i) {
        const length = this.heap.length;
        const element = this.heap[i];
        while (true) {
            let leftChildIdx = 2 * i + 1;
            let rightChildIdx = 2 * i + 2;
            let leftChild, rightChild;
            let swap = null;

            // Проверка левого потомка
            if (leftChildIdx < length) {
                leftChild = this.heap[leftChildIdx];
                if (leftChild.priority < element.priority) {
                    swap = leftChildIdx;
                }
            }
            // Проверка правого потомка
            if (rightChildIdx < length) {
                rightChild = this.heap[rightChildIdx];
                if (
                    (swap === null && rightChild.priority < element.priority) || 
                    (swap !== null && rightChild.priority < leftChild.priority)
                ) {
                    swap = rightChildIdx;
                }
            }

            if (swap === null) break; // Свойство кучи восстановлено
            this.heap[i] = this.heap[swap];
            this.heap[swap] = element;
            i = swap;
        }
    }
}
""")
    add_heading(doc, "6.2. Метод выполнения алгоритма Дейкстры", 2)
    add_code_block(doc, """
run(startNodeId, endNodeId) {
    const startTime = performance.now(); // Фиксация времени начала работы

    // Инициализация структур хранения промежуточных результатов
    this.distances = new Map();
    this.predecessors = new Map();
    this.visited = new Set();
    this.operationCount = 0;

    // Валидация входных данных (выбрасывает исключения при ошибках)
    this._validateInput(startNodeId, endNodeId);
    this._validateWeights(); 

    const adjList = this.graph.getAdjacencyList();
    const allNodes = Array.from(this.graph.nodes.keys());

    // Инициализация расстояний: 0 для старта, Infinity для остальных
    allNodes.forEach(id => {
        this.distances.set(id, id === startNodeId ? 0 : Infinity);
        this.predecessors.set(id, null);
    });

    // Создание очереди и добавление стартовой вершины
    const pq = new PriorityQueue();
    pq.enqueue(startNodeId, 0);

    // Основной вычислительный цикл
    while (!pq.isEmpty()) {
        this.operationCount++;
        // Извлечение вершины u с минимальным расстоянием
        const { element: u, priority: currentDist } = pq.dequeue();

        // Пропуск устаревших записей в куче
        if (this.visited.has(u)) continue;
        
        // Пометка вершины как окончательно обработанной
        this.visited.add(u);

        // Условие досрочного выхода, если найдена целевая вершина
        if (endNodeId && u === endNodeId) break;

        // Релаксация смежных рёбер
        const neighbors = adjList.get(u) || [];
        for (const { nodeId: v, weight } of neighbors) {
            this.operationCount++;
            if (this.visited.has(v)) continue;

            const newDist = this.distances.get(u) + weight;
            // Если найден путь короче, обновляем данные и добавляем в кучу
            if (newDist < this.distances.get(v)) {
                this.distances.set(v, newDist);
                this.predecessors.set(v, u);
                pq.enqueue(v, newDist);
            }
        }
    }

    // Реконструкция итогового маршрута
    const path = this._reconstructPath(startNodeId, endNodeId);
    const executionTime = performance.now() - startTime;

    // Возврат агрегированного результата работы
    return { 
        path, 
        distances: this.distances, 
        predecessors: this.predecessors,
        operationCount: this.operationCount, 
        executionTime 
    };
}
""")

    # ===================================================================
    # 7. РАСПЕЧАТКИ ВХОДНОГО И ВЫХОДНОГО ФАЙЛОВ
    # ===================================================================
    add_heading(doc, "7. Распечатки содержимого входного и выходного файлов программ", 1)
    add_paragraph(
        doc,
        "В данном разделе приводятся полные распечатки файлов, используемых для обмена данными. "
        "В качестве наглядного примера используется неориентированный тестовый граф, состоящий из "
        "четырех вершин (идентификаторы n0, n1, n2, n3). Выполняется поиск кратчайшего пути от узла 0 "
        "до узла 3."
    )
    add_heading(doc, "7.1. Входной файл (Импорт графа)", 2)
    add_paragraph(
        doc,
        "Входной файл представляет собой валидный JSON-документ, описывающий топологию и визуальные "
        "характеристики графа."
    )
    add_code_block(doc, """
{
  "directed": false,
  "nodes": [
    {
      "id": "n0",
      "label": "0",
      "x": 100,
      "y": 200
    },
    {
      "id": "n1",
      "label": "1",
      "x": 300,
      "y": 120
    },
    {
      "id": "n2",
      "label": "2",
      "x": 300,
      "y": 280
    },
    {
      "id": "n3",
      "label": "3",
      "x": 500,
      "y": 200
    }
  ],
  "edges": [
    {
      "id": "e0",
      "source": "n0",
      "target": "n1",
      "weight": 4
    },
    {
      "id": "e1",
      "source": "n0",
      "target": "n2",
      "weight": 2
    },
    {
      "id": "e2",
      "source": "n2",
      "target": "n1",
      "weight": 1
    },
    {
      "id": "e3",
      "source": "n1",
      "target": "n3",
      "weight": 3
    }
  ]
}
""")
    add_heading(doc, "7.2. Выходной файл (Экспорт результатов)", 2)
    add_paragraph(
        doc,
        "Выходной файл генерируется автоматически по завершении расчетов. В нем содержится массив "
        "найденного пути, итоговая стоимость маршрута, затраченное время, а также слепки таблиц "
        "расстояний и предшественников для отладки."
    )
    add_code_block(doc, """
{
  "path": [
    "0",
    "2",
    "1",
    "3"
  ],
  "distance": 6,
  "executionTime": 0.118,
  "operationCount": 24,
  "distances": {
    "0": 0,
    "1": 3,
    "2": 2,
    "3": 6
  },
  "predecessors": {
    "1": "2",
    "2": "0",
    "3": "1"
  }
}
""")

    # ===================================================================
    # 8. СПИСОК ИСТОЧНИКОВ
    # ===================================================================
    add_heading(doc, "8. Список используемых в работе литературных и иных информационных источников", 1)
    
    references = [
        "Ахо А. В., Хопкрофт Д., Ульман Дж. Д. Структуры данных и алгоритмы – М.: Вильямс, 2003. - 384 с., ил.",
        "Кнут Д. Э. Искусство программирования, том 1. Основные алгоритмы, 3-е издание – М.: Вильямс, 2000. - 720 с., ил.",
        "Кнут Д. Э. Искусство программирования, том 2. Получисленные методы, 3-е издание – М.: Вильямс, 2000. - 832 с., ил.",
        "Кнут Д. Э. Искусство программирования, том 3. Сортировка и поиск, 2-е издание – М.: Вильямс, 2000. - 832 с., ил.",
        "Кормен Т. Х., Лейзерсон Ч. И., Ривест Р. Л., Штайн К. Алгоритмы: построение и анализ 2-е издание – М.: Питер, 2004. – 1296 с., ил.",
        "Макконелл Дж. Основы современных алгоритмов 2-е изд. – М.: Техносфера, 2004. – 368 с.",
        "Немнюгин С. А. Turbo Pascal. Программирование на языке высокого уровня: Учебник для вузов. 2-е изд. - М.: Питер, 2006. – 544 с., ил.",
        "Немнюгин С. А. Turbo Pascal: Практикум. 2-е изд. - М.: Питер, 2006. – 272 с., ил.",
        "Павловская Т. А. Паскаль. Программирование на языке высокого уровня: Учебник для вузов - М.: Питер, 2005. – 400 с., ил.",
        "Павловская Т. А. Паскаль. Программирование на языке высокого уровня: Практикум - М.: Питер, 2005. – 320 с., ил.",
        "Адаменко А. Pascal на примерах из математики – СПб.: BHV, 2004. – 416 с., ил.",
        "Долинский М. С. Алгоритмизация и программирование на Turbo Pascal: от простых до олимпиадных задач: Учебное пособие - М.: Питер, 2004. – 240 с., ил.",
        "Долинский М. С. Решение сложных и олимпиадных задач по программированию: Учебное пособие- М.: Питер, 2006. – 368 с.",
        "Культин Н. Turbo Pascal в задачах и примерах – СПб.: BHV, 2005. – 256 с., ил.",
        "Меньшиков Ф. В. Олимпиадные задачи по программированию (+CD) - М.: Питер, 2005. – 320 с.",
        "Потопахин В. Turbo Pascal. Освой на примерах – СПб.: BHV, 2004. – 240 с., ил.",
        "Потопахин В. Turbo Pascal. Решение сложных задач – СПб.: BHV, 2005. – 340 с., ил.",
        "Протасов И. Д. Лекции по вычислительной математике– М.: Гелиос-АРВ, 2004. – 184 с., ил.",
        "Протасов И. Д. Теория игр и исследование операций: Учебное пособие, 2-е изд.– М.: Гелиос-АРВ, 2006. – 368 с., ил.",
        "Совертков П. Занимательное компьютерное моделирование в элементарной математике – М.: Гелиос-АРВ, 2004. – 384 с., ил."
    ]
    
    # Sort or just add them sequentially. The requirement implies numbering them.
    add_numbered(doc, references)

    doc.save(DOCX_PATH)

if __name__ == "__main__":
    build_docx_massive()
    print("Massive DOCX built.")
